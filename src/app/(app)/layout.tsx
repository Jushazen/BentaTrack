// Signed-in app shell (navigation, theme toggle, sync status). Implemented in leaf 3.1.
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <main>{children}</main>;
}
